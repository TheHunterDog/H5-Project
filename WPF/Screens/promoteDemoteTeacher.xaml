<Window x:Class="WPF.Screens.promoteDemoteTeacher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Screens"
        mc:Ignorable="d"
        Title="promoteDemoteTeacher" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label x:Name="Confirmation" 
               Grid.ColumnSpan="2"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="24"
               />
        <Button Name="Submit" 
                Click="SubmitBtn" 
                Grid.Row="1" 
                Content="submit"
                Width="150"
                Height="50"
                />
        <Button Name="cancel" 
                Click="CancelBtn" 
                Grid.Row="1"
                Grid.Column="1"
                Content="Cancel"
                Width="150"
                Height="50"
                />
    </Grid>
</Window>
